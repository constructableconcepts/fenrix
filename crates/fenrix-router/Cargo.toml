[package]
name = "fenrix-router"
version = "0.1.0"
edition = "2021"

[dependencies]
fenrix-core = { path = "../../crates/fenrix-core" }
wasm-bindgen = "0.2"
web-sys = { version = "0.3", features = [
    "Location",
    "Window",
    "HashChangeEvent",
    "Event",
    "EventTarget",
]}